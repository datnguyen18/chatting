//import liraries
import React, { Component } from 'react';
import { LoginAndRegister } from './MainRoot';

// create a component
export default class MainScreen extends Component {
    render() {
        return (
            <LoginAndRegister />
        );
    }
}